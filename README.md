# FirstSubstringOccurrence
Program indicates whether there is a substring in the given sentence. It returns -1 if substring does not appear in the longer sentence. 
If substring appears the program returns the index of the substring in the longer term.

Use of two solutions: one with the foreach loop and second with the for loop.
Use of if statements.
